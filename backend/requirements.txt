# ESG Mefali — Backend dependencies
# Python 3.12+ (testé aussi avec 3.14)
# Versions minimales pinées ; la résolution privilégie les versions compatibles
# avec l'interpréteur installé.

# Web framework
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.40
python-multipart>=0.0.20  # F12 - multipart upload (documents projet)
pypdf>=4.0,<6  # F22 - extraction texte natif PDF (sync OCR MVP)

# ORM + migrations
sqlalchemy>=2.0.36,<2.1
alembic>=1.14,<1.18
psycopg[binary]>=3.2.3
pgvector>=0.3.6,<0.5

# Config / validation
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
python-dotenv>=1.0.1,<2

# Clients externes (sans appel en F01)
openai>=1.55
httpx>=0.27,<0.29

# LLM orchestration (préparé pour F14)
langgraph>=0.2.50
langchain>=0.3.7

# Embeddings (préparé pour F18) — appel direct via httpx en F01.
# Le SDK voyageai (incompatible Python 3.14 au moment de F01) sera ajouté
# en F18 si besoin d'un wrapper officiel.
# voyageai>=0.3.1,<0.4

# Auth (F02)
passlib[bcrypt]>=1.7.4,<2
python-jose[cryptography]>=3.3.0,<4
slowapi>=0.1.9,<0.2
sse-starlette>=2.1,<3  # F38 - SSE keepalive (extended by F41)
email-validator>=2.2,<3
aiosmtplib>=3.0,<4

# F03 — Source anti-hallucination middleware decision cache
cachetools>=5.3,<6

# F58 — Lang detection guardrails
langdetect>=1.0.9,<2.0

# F24 — Rapport de Conformité PDF (reportlab pur Python + matplotlib pour radar)
reportlab>=4.0,<5
matplotlib>=3.8,<4

# === Dev ===
pytest>=8.3,<9
pytest-asyncio>=0.24
pytest-cov>=6.0
ruff>=0.7
